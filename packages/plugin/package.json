{
  "name": "flow-launcher-chzzk",
  "version": "1.1.1",
  "description": "CHZZK plugin for Flow Launcher",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --outfile=dist/main.js --define:process.env.PROXY_AUTH_KEY='\"$PROXY_AUTH_KEY\"'",
    "lint": "oxlint"
  },
  "dependencies": {
    "dotenv": "^16.6.0",
    "open": "^8.4.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.4",
    "esbuild": "^0.25.7"
  }
}
